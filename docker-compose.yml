version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - '4000:4000'
    environment:
      - MONGO_URI
      - JWT_SECRET
    depends_on:
      - mongo
  frontend:
    build: ./frontend
    ports:
      - '5173:5173'
  mongo:
    image: mongo:6
    ports:
      - '27017:27017'
